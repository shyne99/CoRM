version: '3'
services:
  db:
  #for arm -> image: arm32v7/postgres:9.4 
    image: postgres:9.4
    volumes:
      - ./tmp/db:/var/lib/postgresql/data
  web:
    build: .
    command:  bundle exec rails s -p 3000 -b '0.0.0.0'
    ports:
      - "3000:3000"
    depends_on:
      - db